diff --git a/run-command.c b/run-command.c
index e70a8a3..a4070e3 100644
--- a/run-command.c
+++ b/run-command.c
@@ -1,6 +1,7 @@
 #define DISABLE_SIGN_COMPARE_WARNINGS
 
 #include "git-compat-util.h"
+#include "abspath.h"
 #include "run-command.h"
 #include "environment.h"
 #include "exec-cmd.h"
@@ -136,6 +137,11 @@ int is_executable(const char *name)
 	    !S_ISREG(st.st_mode))
 		return 0;
 
+#ifdef DOLLY
+	/* The module loader validates executables; Dolly has no execute bits. */
+	return 1;
+#endif
+
 #if defined(GIT_WINDOWS_NATIVE)
 	/*
 	 * On Windows there is no executable bit. The file extension
@@ -745,7 +751,95 @@ fail_pipe:
 	if (cmd->odb_to_close)
 		odb_close(cmd->odb_to_close);
 
-#ifndef GIT_WINDOWS_NATIVE
+#ifdef DOLLY
+{
+	int fhin = 0, fhout = 1, fherr = 2, null_fd = -1;
+	char **childenv = NULL;
+	char *cwd = NULL, *path = NULL;
+	struct strvec argv = STRVEC_INIT;
+	dolly_process_fd_mapping mappings[3];
+	uint32_t inheritance = cmd->close_fd_above_stderr
+		? DOLLY_PROCESS_INHERIT_FDS_NONE : DOLLY_PROCESS_INHERIT_FDS_ALL;
+
+	cmd->pid = -1;
+	if (prepare_cmd(&argv, cmd) < 0)
+		goto dolly_spawn_done;
+	if (argv.nr > INT_MAX) {
+		errno = E2BIG;
+		goto dolly_spawn_done;
+	}
+	trace_argv_printf(&argv.v[1], "trace: start_command:");
+	if (cmd->no_stdin || cmd->no_stdout || cmd->no_stderr) {
+		null_fd = open("/dev/null", O_RDWR | O_CLOEXEC);
+		if (null_fd < 0)
+			goto dolly_spawn_done;
+	}
+	if (cmd->no_stdin)
+		fhin = null_fd;
+	else if (need_in)
+		fhin = fdin[0];
+	else if (cmd->in)
+		fhin = cmd->in;
+	if (cmd->no_stderr)
+		fherr = null_fd;
+	else if (need_err)
+		fherr = fderr[1];
+	else if (cmd->err > 1)
+		fherr = cmd->err;
+	if (cmd->no_stdout)
+		fhout = null_fd;
+	else if (cmd->stdout_to_stderr)
+		fhout = fherr;
+	else if (need_out)
+		fhout = fdout[1];
+	else if (cmd->out > 1)
+		fhout = cmd->out;
+
+	/* These owned descriptors are closed by the fork branch before exec. */
+	{
+		int owned[] = { need_in ? fdin[0] : -1,
+			need_out ? fdout[1] : -1, need_err ? fderr[1] : -1,
+			cmd->in, cmd->out, cmd->err, null_fd };
+		for (size_t i = 0; i < ARRAY_SIZE(owned); i++) {
+			if (owned[i] > 2) {
+				int flags = fcntl(owned[i], F_GETFD);
+				if (flags < 0 || fcntl(owned[i], F_SETFD, flags | FD_CLOEXEC) < 0)
+					goto dolly_spawn_done;
+			}
+		}
+	}
+	mappings[0] = (dolly_process_fd_mapping){fhin, 0};
+	mappings[1] = (dolly_process_fd_mapping){fhout, 1};
+	mappings[2] = (dolly_process_fd_mapping){fherr, 2};
+	childenv = prep_childenv(cmd->env.v);
+	if (cmd->dir)
+		cwd = absolute_pathdup(cmd->dir);
+	path = cwd && !is_absolute_path(argv.v[1])
+		? xstrfmt("%s/%s", cwd, argv.v[1])
+		: absolute_pathdup(argv.v[1]);
+	cmd->pid = dolly_spawn_mapped(path, argv.nr - 1,
+		(char **)argv.v + 1, childenv, cwd, inheritance, mappings, 3, -1);
+	if (cmd->pid == -ENOEXEC)
+		cmd->pid = dolly_spawn_mapped(argv.v[0], argv.nr,
+			(char **)argv.v, childenv, cwd, inheritance, mappings, 3, -1);
+	if (cmd->pid < 0) {
+		errno = -cmd->pid;
+		cmd->pid = -1;
+	}
+dolly_spawn_done:
+	failed_errno = errno;
+	if (cmd->pid < 0 && (!cmd->silent_exec_failure || errno != ENOENT))
+		error_errno("cannot spawn %s", cmd->args.v[0]);
+	if (cmd->pid >= 0 && cmd->clean_on_exit)
+		mark_child_for_cleanup(cmd->pid, cmd);
+	if (null_fd >= 0)
+		close(null_fd);
+	strvec_clear(&argv);
+	free(childenv);
+	free(cwd);
+	free(path);
+}
+#elif !defined(GIT_WINDOWS_NATIVE)
 {
 	int notify_pipe[2];
 	int null_fd = -1;
diff --git a/fetch-pack.c b/fetch-pack.c
index 120e01f..ad06ebf 100644
--- a/fetch-pack.c
+++ b/fetch-pack.c
@@ -892,6 +892,7 @@ static int everything_local(struct fetch_pack_args *args,
 	return retval;
 }
 
+#ifndef DOLLY
 static int sideband_demux(int in UNUSED, int out, void *data)
 {
 	int *xd = data;
@@ -901,6 +902,7 @@ static int sideband_demux(int in UNUSED, int out, void *data)
 	close(out);
 	return ret;
 }
+#endif
 
 static void create_promisor_file(const char *keep_name,
 				 struct ref **sought, int nr_sought)
@@ -972,6 +974,22 @@ static int get_pack(struct fetch_pack_args *args,
 
 	memset(&demux, 0, sizeof(demux));
 	if (use_sideband) {
+#ifdef DOLLY
+		struct tempfile *spool = mks_tempfile_t("git-fetch-pack-XXXXXX");
+
+		/* Receive before indexing: a pipe would deadlock without a reader. */
+		if (!spool)
+			die_errno(_("fetch-pack: unable to create pack spool"));
+		demux.out = dup(get_tempfile_fd(spool));
+		if (demux.out < 0)
+			die_errno(_("fetch-pack: unable to duplicate pack spool"));
+		if (delete_tempfile(&spool))
+			die_errno(_("fetch-pack: unable to unlink pack spool"));
+		if (recv_sideband("fetch-pack", xd[0], demux.out))
+			die(_("error in sideband demultiplexer"));
+		if (lseek(demux.out, 0, SEEK_SET) < 0)
+			die_errno(_("fetch-pack: unable to rewind pack spool"));
+#else
 		/* xd[] is talking with upload-pack; subprocess reads from
 		 * xd[0], spits out band#2 to stderr, and feeds us band#1
 		 * through demux->out.
@@ -982,6 +1000,7 @@ static int get_pack(struct fetch_pack_args *args,
 		demux.isolate_sigpipe = 1;
 		if (start_async(&demux))
 			die(_("fetch-pack: unable to fork off sideband demultiplexer"));
+#endif
 	}
 	else
 		demux.out = xd[0];
@@ -1098,8 +1117,10 @@ static int get_pack(struct fetch_pack_args *args,
 			ret == 0;
 	else
 		die(_("%s failed"), cmd_name);
+#ifndef DOLLY
 	if (use_sideband && finish_async(&demux))
 		die(_("error in sideband demultiplexer"));
+#endif
 
 	sigchain_pop(SIGPIPE);
 
diff --git a/send-pack.c b/send-pack.c
--- a/send-pack.c
+++ b/send-pack.c
@@ -7,6 +7,9 @@
 #include "odb.h"
 #include "pkt-line.h"
 #include "sideband.h"
+#ifdef DOLLY
+#include "tempfile.h"
+#endif
 #include "run-command.h"
 #include "remote.h"
 #include "connect.h"
@@ -281,6 +284,7 @@
 	return ret;
 }
 
+#ifndef DOLLY
 static int sideband_demux(int in UNUSED, int out, void *data)
 {
 	int *fd = data, ret;
@@ -290,6 +294,7 @@
 	close(out);
 	return ret;
 }
+#endif
 
 static int advertise_shallow_grafts_cb(const struct commit_graft *graft, void *cb)
 {
@@ -727,6 +732,7 @@
 		packet_flush(out);
 	}
 
+#ifndef DOLLY
 	if (use_sideband && cmds_sent) {
 		memset(&demux, 0, sizeof(demux));
 		demux.proc = sideband_demux;
@@ -737,6 +743,7 @@
 			die("send-pack: unable to fork off sideband demultiplexer");
 		in = demux.out;
 	}
+#endif
 
 	packet_reader_init(&reader, in, NULL, 0,
 			   PACKET_READ_CHOMP_NEWLINE |
@@ -755,13 +762,19 @@
 			 * as well as marking refs with their remote status (if
 			 * we get one).
 			 */
+#ifdef DOLLY
+			if (status_report && !use_sideband)
+#else
 			if (status_report)
+#endif
 				receive_status(r, &reader, remote_refs);
 
+#ifndef DOLLY
 			if (use_sideband) {
 				close(demux.out);
 				finish_async(&demux);
 			}
+#endif
 			fd[1] = -1;
 
 			ret = -1;
@@ -774,6 +787,31 @@
 	if (args->stateless_rpc && cmds_sent)
 		packet_flush(out);
 
+#ifdef DOLLY
+	if (use_sideband && cmds_sent) {
+		struct tempfile *spool = mks_tempfile_t("git-send-pack-XXXXXX");
+
+		/* The complete request is sent before receiving its sideband report. */
+		if (!spool)
+			die_errno("send-pack: unable to create status spool");
+		demux.out = dup(get_tempfile_fd(spool));
+		if (demux.out < 0)
+			die_errno("send-pack: unable to duplicate status spool");
+		if (delete_tempfile(&spool))
+			die_errno("send-pack: unable to unlink status spool");
+		if (recv_sideband("send-pack", fd[0], demux.out)) {
+			close(demux.out);
+			ret = -1;
+			goto out;
+		}
+		if (lseek(demux.out, 0, SEEK_SET) < 0)
+			die_errno("send-pack: unable to rewind status spool");
+		packet_reader_init(&reader, demux.out, NULL, 0,
+				   PACKET_READ_CHOMP_NEWLINE |
+				   PACKET_READ_DIE_ON_ERR_PACKET);
+	}
+#endif
+
 	if (status_report && cmds_sent)
 		ret = receive_status(r, &reader, remote_refs);
 	else
@@ -783,10 +821,12 @@
 
 	if (use_sideband && cmds_sent) {
 		close(demux.out);
+#ifndef DOLLY
 		if (finish_async(&demux)) {
 			error("error in sideband demultiplexer");
 			ret = -1;
 		}
+#endif
 	}
 
 	if (ret < 0)
