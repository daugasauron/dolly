diff --git a/include/uv/unix.h b/include/uv/unix.h
index c6ba419..e50631f 100644
--- a/include/uv/unix.h
+++ b/include/uv/unix.h
@@ -45,7 +45,9 @@
 
 #include "uv/threadpool.h"
 
-#if defined(__linux__)
+#if defined(__wasm64__)
+# include "uv/dolly.h"
+#elif defined(__linux__)
 # include "uv/linux.h"
 #elif defined (__MVS__)
 # include "uv/os390.h"
diff --git a/src/threadpool.c b/src/threadpool.c
index 2a129a5..ec425fc 100644
--- a/src/threadpool.c
+++ b/src/threadpool.c
@@ -29,6 +29,11 @@
 
 #define MAX_THREADPOOL_SIZE 1024
 
+static void uv__cancelled(struct uv__work* w) {
+  abort();
+}
+
+#ifndef DOLLY
 static uv_once_t once = UV_ONCE_INIT;
 static uv_cond_t cond;
 static uv_mutex_t mutex;
@@ -46,10 +51,6 @@ static unsigned int slow_work_thread_threshold(void) {
   return (nthreads + 1) / 2;
 }
 
-static void uv__cancelled(struct uv__work* w) {
-  abort();
-}
-
 
 /* To avoid deadlock with uv_cancel() it's crucial that the worker
  * never holds the global mutex and the loop-local mutex at the same time.
@@ -273,16 +274,28 @@ static void init_once(void) {
 }
 
 
+#else
+void uv__threadpool_cleanup(void) {
+}
+#endif
+
 void uv__work_submit(uv_loop_t* loop,
                      struct uv__work* w,
                      enum uv__work_kind kind,
                      void (*work)(struct uv__work* w),
                      void (*done)(struct uv__work* w, int status)) {
+#ifndef DOLLY
   uv_once(&once, init_once);
+#endif
   w->loop = loop;
   w->work = work;
   w->done = done;
+#ifdef DOLLY
+  uv__queue_insert_tail(&loop->wq, &w->wq);
+  uv_async_send(&loop->wq_async);
+#else
   post(&w->wq, kind);
+#endif
 }
 
 
@@ -290,6 +303,12 @@ void uv__work_submit(uv_loop_t* loop,
  * that go through io_uring instead of the thread pool.
  */
 static int uv__work_cancel(uv_loop_t* loop, uv_req_t* req, struct uv__work* w) {
+#ifdef DOLLY
+  if (uv__queue_empty(&w->wq) || w->work == NULL || w->work == uv__cancelled)
+    return UV_EBUSY;
+  w->work = uv__cancelled;
+  return 0;
+#else
   int cancelled;
 
   uv_once(&once, init_once);  /* Ensure |mutex| is initialized. */
@@ -313,6 +332,7 @@ static int uv__work_cancel(uv_loop_t* loop, uv_req_t* req, struct uv__work* w) {
   uv_mutex_unlock(&loop->wq_mutex);
 
   return 0;
+#endif
 }
 
 
@@ -337,6 +357,12 @@ void uv__work_done(uv_async_t* handle) {
 
     w = container_of(q, struct uv__work, wq);
     err = (w->work == uv__cancelled) ? UV_ECANCELED : 0;
+#ifdef DOLLY
+    uv__queue_init(q);
+    if (err == 0)
+      w->work(w);
+    w->work = NULL;
+#endif
     w->done(w, err);
     nevents++;
   }
diff --git a/src/unix/posix-poll.c b/src/unix/posix-poll.c
index 5b7d75c..4293f25 100644
--- a/src/unix/posix-poll.c
+++ b/src/unix/posix-poll.c
@@ -42,6 +42,9 @@ int uv__platform_loop_init(uv_loop_t* loop) {
 }
 
 void uv__platform_loop_delete(uv_loop_t* loop) {
+#ifdef DOLLY
+  uv__dolly_process_cleanup(loop);
+#endif
   uv__free(loop->poll_fds);
   loop->poll_fds = NULL;
 }
@@ -49,6 +52,10 @@ void uv__platform_loop_delete(uv_loop_t* loop) {
 
 int uv__io_fork(uv_loop_t* loop) {
+#ifdef DOLLY
+  return UV_ENOSYS;
+#else
   uv__platform_loop_delete(loop);
   return uv__platform_loop_init(loop);
+#endif
 }
 
diff --git a/src/unix/thread.c b/src/unix/thread.c
--- a/src/unix/thread.c
+++ b/src/unix/thread.c
@@ -139,6 +139,9 @@ int uv_thread_create_ex(uv_thread_t* tid,
                         const uv_thread_options_t* params,
                         void (*entry)(void *arg),
                         void *arg) {
+#ifdef DOLLY
+  return UV_ENOSYS;
+#else
   int err;
   pthread_attr_t* attr;
   pthread_attr_t attr_storage;
@@ -184,6 +187,7 @@ int uv_thread_create_ex(uv_thread_t* tid,
     pthread_attr_destroy(attr);
 
   return UV__ERR(err);
+#endif
 }
 
 #if UV__CPU_AFFINITY_SUPPORTED
