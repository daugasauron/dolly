--- a/src/AM.cpp
+++ b/src/AM.cpp
@@ -300,4 +300,5 @@
 int main(int argc, char **argv)
 {
+	if (dolly_rts_prepare()) return 1;
 	if (!sys.set_game_dir())
 		return 1;
@@ -319,5 +320,6 @@
 	if( !cmd_line.init(argc, argv) )
 		return 1;
+	dolly_rts_configure();
 
 #ifdef ENABLE_INTRO_VIDEO
 	//----------- play movie ---------------//
@@ -366,3 +368,5 @@
-	switch( cmd_line.startup_mode )
+	if (dolly_rts_player()) dolly_rts_run();
+	else if (dolly_rts_replay(argc, argv)) {}
+	else switch( cmd_line.startup_mode )
 	{
 	case STARTUP_NORMAL:
--- a/include/OBATTLE.h
+++ b/include/OBATTLE.h
@@ -48,1 +48,1 @@
-	void	run_replay();
+	int	run_replay(char *filename = 0);
--- a/src/OBATTLE.cpp
+++ b/src/OBATTLE.cpp
@@ -665,5 +665,5 @@
-void Battle::run_replay()
+int Battle::run_replay(char *filename)
 {
 	NewNationPara *mpGame = (NewNationPara *)mem_add(sizeof(NewNationPara)*MAX_NATION);
 	int mpPlayerCount = 0;
-	FilePath full_path(sys.dir_config);
+	FilePath full_path(filename ? filename : sys.dir_config);
@@ -672,9 +672,9 @@
-	full_path += "NONAME.RPL";
-	if( full_path.error_flag )
-		return;
-
-	if( !remote.init_replay_load(full_path, mpGame, &mpPlayerCount) )
-		return;
+	if (!filename) full_path += "NONAME.RPL";
+	if( full_path.error_flag || !remote.init_replay_load(full_path, mpGame, &mpPlayerCount) )
+	{
+		mem_del(mpGame);
+		return 0;
+	}
 
 	game.init();
-	game.game_mode = GAME_DEMO;
+	game.game_mode = dolly_rts_replay_player() ? GAME_MULTI_PLAYER : GAME_DEMO;
@@ -683,5 +683,8 @@
 	mem_del(mpGame);
+	if (dolly_rts_replay_player()) vga.flip();
+	const int result = remote.is_replay_end() ? 1 : 2;
 	remote.deinit();
 	game.deinit();
 	config = tmpConfig;
+	return result;
 }
--- a/src/ONATIONA.cpp
+++ b/src/ONATIONA.cpp
@@ -184,1 +184,3 @@
+	if (dolly_rts_replay_player() == nationRecno) nationType = NATION_OWN;
+
 	if( nationType == NATION_OWN )
--- a/src/OREMOTE.cpp
+++ b/src/OREMOTE.cpp
@@ -191,1 +191,1 @@
-	return connectivity_mode == MODE_MP_ENABLED;
+	return connectivity_mode == MODE_MP_ENABLED || dolly_rts_replay_detecting();
@@ -241,1 +241,2 @@
+	if (dolly_rts_replay_player()) return dolly_rts_replay_player();
 	err_when(connectivity_mode != MODE_MP_ENABLED);
--- a/src/OREMOTE2.cpp
+++ b/src/OREMOTE2.cpp
@@ -186,4 +186,5 @@
 char* Remote::new_send_queue_msg(uint32_t msgId, int dataSize)
 {
+	if (dolly_rts_replay_player()) return dolly_rts_replay_discard(dataSize);
 	char *dataPtr = send_queue[0].reserve(sizeof(short) + sizeof(uint32_t) + dataSize);
 
--- a/src/OVGA.cpp
+++ b/src/OVGA.cpp
@@ -357,6 +357,8 @@
-void Vga::handle_messages()
+void Vga::handle_messages(bool playerInput)
 {
    SDL_Event event;
+   dolly_rts_poll();
+   if (playerInput) dolly_rts_input(window, sys.frame_count);
 
    while( SDL_PollEvent(&event) )
    {
@@ -804,1 +806,1 @@
-   if (cur_ticks > ticks + 17 || cur_ticks < ticks) {
+   if (cur_ticks > ticks + 17 || cur_ticks < ticks || dolly_rts_replay_eof()) {
@@ -807,6 +809,7 @@
       SDL_UpdateTexture(texture, NULL, target->pixels, target->pitch);
       SDL_RenderClear(renderer);
       SDL_RenderCopy(renderer, texture, NULL, NULL);
+      dolly_rts_frame(renderer, sys.frame_count);
       SDL_RenderPresent(renderer);
    }
 }
--- a/include/multiplayer.h
+++ b/include/multiplayer.h
@@ -185,3 +185,4 @@
 	void   init(ProtocolType);
+	void   init_local(uint32_t player);
 	void   deinit();
 	bool   is_initialized() const { return init_flag != 0; }
--- a/src/enet/multiplayer.cpp
+++ b/src/enet/multiplayer.cpp
@@ -1057,4 +1057,5 @@
 int MultiPlayer::is_player_connecting(uint32_t playerId)
 {
+	if (dolly_rts_player()) return playerId == 1 || playerId == 2;
 #ifdef USE_ENET
 	ENetPeer *peer;
@@ -1085,4 +1086,5 @@
 int MultiPlayer::send(uint32_t to, void *data, uint32_t msg_size)
 {
+	if (dolly_rts_player()) return dolly_rts_send(to, data, msg_size);
 #ifdef USE_ENET
 	ENetPacket *packet;
@@ -1317,4 +1319,5 @@
 char *MultiPlayer::receive(uint32_t *from, uint32_t *size, int *sysMsgCount)
 {
+	if (dolly_rts_player()) return dolly_rts_receive(from, size, sysMsgCount);
 #ifdef USE_ENET
 	int ret;
--- a/src/OSYS.cpp
+++ b/src/OSYS.cpp
@@ -797,1 +797,3 @@
+         dolly_rts_replay_detect(true);
          detect();
+         dolly_rts_replay_detect(false);
@@ -1423,4 +1423,5 @@
       if ( !ec_remote.is_player_valid(nationRecno) )
       {
+         if (dolly_rts_player()) dolly_rts_transport_failed();
          DEBUG_LOG("Connection Lost");
          DEBUG_LOG(nationRecno);
@@ -1479,1 +1481,2 @@
+   if (dolly_rts_replay_player()) return dolly_rts_replay_next_frame();
    //----- special modes: 0-frozen, 9-fastest possible -----//
@@ -1680,4 +1681,5 @@
       config.opaque_report = !config.opaque_report;
+      if (dolly_rts_player() || dolly_rts_replay_player()) return;
 
       if( config.opaque_report )
          box.msg( _("Opaque report mode") );
@@ -2621,4 +2623,5 @@
 void Sys::set_speed(int frameSpeed, int remoteCall)
 {
+   if (dolly_rts_player() || dolly_rts_replay_player()) return;
    short requested_speed;
 
@@ -2664,4 +2667,5 @@
 void Sys::capture_screen()
 {
+   if (dolly_rts_player() || dolly_rts_replay_player()) return;
    FilePath full_path(dir_config);
    const char filename_template[] = "7KXXXXXX.BMP";
@@ -2767,4 +2771,5 @@
 void Sys::save_game()
 {
+   if (dolly_rts_player() || dolly_rts_replay_player()) return;
    if( nation_array.player_recno==0 )     // cannot save game when the player's kingdom has been destroyed
       return;
--- a/src/OSYS2.cpp
+++ b/src/OSYS2.cpp
@@ -575,1 +575,1 @@
-		if( !remote.is_enable() )		// help is only available in a single player game as it has to pause the game
+		if( !remote.is_enable() && !dolly_rts_replay_player() )
--- a/src/OSYS.cpp
+++ b/src/OSYS.cpp
@@ -1198,3 +1198,3 @@
-   if( remote.is_enable() )
+   if( remote.is_enable() && !dolly_rts_replay_player() )
    {
       //yield_wsock_msg();
--- a/src/OINGMENU.cpp
+++ b/src/OINGMENU.cpp
@@ -74,4 +74,6 @@
 void InGameMenu::enter(char untilExitFlag)
 {
+   // Arena lifecycle belongs to the spectator, not the players.
+   if (dolly_rts_player() || dolly_rts_replay_player()) return;
    if( active_flag )
       return;
--- a/src/OOPTMENU.cpp
+++ b/src/OOPTMENU.cpp
@@ -93,4 +93,5 @@
 void OptionMenu::enter(char untilExitFlag)
 {
+	if (dolly_rts_player() || dolly_rts_replay_player()) return;
 	if( is_active() )
 		return;
--- a/src/OGAMEND.cpp
+++ b/src/OGAMEND.cpp
@@ -78,4 +78,8 @@
 void Game::game_end(int winNationRecno, int playerDestroyed, int surrenderToNationRecno, int retireFlag)
 {
+	if (dolly_rts_player()) {
+		dolly_rts_finish(winNationRecno, playerDestroyed, surrenderToNationRecno, retireFlag);
+		return;
+	}
 	//--- set scenario as complete if the player wins ---//
 	if( nation_array.player_recno && winNationRecno == nation_array.player_recno )
--- a/include/OVGA.h
+++ b/include/OVGA.h
@@ -117,1 +117,1 @@
-	void   handle_messages();
+	void   handle_messages(bool playerInput = false);
--- a/src/OSYS2.cpp
+++ b/src/OSYS2.cpp
@@ -90,1 +90,4 @@
+	// Advance agent input once per UI update; nested yields only pump events.
+	// Widgets must consume releases before another action moves the pointer.
+	vga.handle_messages(true);
 	mouse.get_event();
