# Day session 2026-09-01

## Python

The python environment seems nice but the package manager doesn't work?

```
dolly:/workspace$ bonnie install numpy
bonnie: PyPI lookup failed: Browser HTTP broker could not connect
bonnie: no portable wheel found for numpy; dependencies and native builds are explicit
slop: status 1
dolly:/workspace$ bonnie install pandas
bonnie: PyPI lookup failed: Browser HTTP broker could not connect
bonnie: no portable wheel found for pandas; dependencies and native builds are explicit
slop: status 1
dolly:/workspace$ bonnie install requests
bonnie: PyPI lookup failed: Browser HTTP broker could not connect
bonnie: no portable wheel found for requests; dependencies and native builds are explicit
slop: status 1
```

```
Cross-Origin Request Blocked: The Same Origin Policy disallows reading the remote resource at https://pypi.org/pypi/requests/json. (Reason: CORS request did not succeed). Status code: (null).
```

If CORS is a problem for official pypi, need a workaround.

Also, the user should be informed at startup that bonnie is the package manager.

Also - improve bonnie and make it as compatible as possible!

## Gamedev

The demo is decent, I like the physics, but a lot of issues.

The font is completely off, not sure if it's inverted upside down or just gibberish, I can see something font like but not sure what it is.

It feels SLOOW, not sure if it's CPU bound or just slow.

I saw that you were missing some things from the browser host, you are free to add:

1: something **small** that solves following:
```
  2. Frame pacing

     There is no guest-visible equivalent of requestAnimationFrame or vsync. The current loop approximates 60 Hz by waiting for events, while raylib’s sleep hooks are stubbed.

     Dolly should expose one narrow operation such as “wait until next display frame,” rather than exposing browser timers or callbacks generally.
```

2: Change the cursor

Currently the cursor is always the "text box cursor". Add something so you can control how the cursor is rendered.

3: Not sure what this means but for: 

```
  3. Rendering performance

     Rendering is currently software-only and involves several full-frame copies:

     raylib framebuffer
         → LoadImageFromScreen
         → Dolly inactive framebuffer
         → browser-side stable buffer
         → canvas putImageData

     At 800×600, one RGBA frame is about 1.9 MB, or 115 MB/s for each full-frame copy at 60 FPS. It works for a POC, but larger resolutions will hurt.

     The clean fix is to let raylib render directly into Dolly’s inactive framebuffer. Dirty rectangles could follow later. WebGPU/WebGL would be much faster, but adding either as a guest
     capability would substantially enlarge the trusted browser interface.
```

I think it makes sense to have an interface to render directly into the framebuffer?

Apart from these 3 points, polish the game and make it cooler.

## General stuff

Iterate inside the actual system using openrouter API key from ~/.openrouter

I currently experience some crashes etc, and there seems to be a lot of obvious issues/bugs still.

Iteratively improve the general user experience with small things.

## Session results

### Python and Bonnie

- Fixed PyPI access through Dolly's existing browser HTTP broker and added a
  dedicated Python resolver behind `/usr/bin/bonnie`.
- Bonnie now resolves recursive pure-Python dependencies, markers and extras,
  detects contradictory constraints, retries downloads, verifies wheel SHA-256,
  and safely installs wheel data.
- Browser acceptance installs Requests and its portable dependency graph.
  CPython now provides an import-compatible `_socket` whose raw operations fail
  locally with `ENOSYS`; NumPy and Pandas remain explicit native-extension ports.
- Python startup now advertises `bonnie install PACKAGE`.

### Gamedev

- Added animation-frame pacing, a closed semantic cursor enum, and bounded
  logical framebuffer sizing without adding a browser import.
- Removed raylib's intermediate `LoadImageFromScreen` allocation/copy and copy
  the rasterizer result directly into Dolly's inactive frame.
- Capped the default software surface at 800×450 while preserving aspect ratio;
  measured demo cadence improved from about 21 to 48 frames per second.
- Fixed RGBA color order, loaded Iosevka correctly, and polished the demo with
  fixed-step physics, player detail, collision particles, and reliable exit.

### Pi and general compatibility

- Ran a final real OpenRouter/DeepSeek audit: twelve requests in about 51 seconds,
  normal Pi exit, then independent Git/build/program/report probes all passed.
- Added separate `/bin/which` plus pinned sbase `cut`, `od`, `true`, and `false`.
- Defined Dolly's no-permission `test -x` rule as “regular executable
  candidate”; the loader remains responsible for Wasm/ABI validation.
- Taught Pi to use its write tool for multiline source and Makefiles instead of
  relying on incorrect `printf '%s'` escape assumptions.
- Made Slop diagnose unsupported `<<` here-documents explicitly after the real
  agent tried one, recovered through the write tool, and completed its task.

Detailed evidence: [`docs/agent-audit-2026-09-01.md`](docs/agent-audit-2026-09-01.md).

### Reproducibility follow-up

- Compared two independent Python image rebuilds record by record. The only
  variance came from CPython's compile-time `__TIME__`, duplicated in `python`
  and `python3`, plus timestamp headers in 21 import-generated `.pyc` files.
- Clang did not apply `SOURCE_DATE_EPOCH` to `__TIME__` in this target, so the
  Python recipe fixes CPython's dedicated build-info `DATE` and `TIME` macros
  directly. It also disables bytecode cache writes during checks and verifies
  that no standard-library `__pycache__` directory was retained.
- Two clean browser rebuilds then produced byte-identical 337,671,288-byte
  snapshots with SHA-256
  `4e26cda6452ba428fbda4653d8e4ebbd26fd94d283800cd8911b71f3d398921b`.
  That image contained 22,406 records and zero `.pyc` files.

### Bonnie transaction and command follow-up

- Bonnie 0.3 now resolves, downloads, SHA-256/CRC/path-checks, and inspects the
  complete chosen graph before it mutates the target. A contradictory graph
  against a fresh `--target` leaves that directory absent.
- Repeated requirements can add extras: the live test installs `requests`
  followed by `requests[socks]` and verifies that PySocks is present.
- Validated wheel `console_scripts` are compiled individually with Dolly's own
  `/bin/cc`; they are ordinary wasm64 files, collision-checked and found through
  PATH. The browser test installs Pytest and executes `/usr/bin/pytest --version`.
- A bare Pytest discovery probe exposed the next honest CPython gap:
  `faulthandler` is intentionally disabled alongside socket/terminal modules.
  It should be evaluated as an upstream source module with command-scoped
  signal semantics, not replaced by a silent no-op.
- The reusable `snapshot:reproducible` command then rebuilt the final Python
  recipe twice. Both outputs were byte-identical at 337,690,607 bytes with
  SHA-256
  `227196366572d47dbdad4102a11ea1c5505f70eee4eed2ff96614874524c2dc1`.

### Dolly CPython target identity

- Compiled CPython's platform object with `PLATFORM="dolly"`. Current urllib3
  therefore no longer treats Dolly as Pyodide or imports its ambient `js`
  capability; no such shim or new browser import was added.
- A narrow pinned CPython patch preserves only the Emscripten-derived behavior
  that Dolly really shares: no native fork/exec, conservative UUID probing,
  Wasm dynamic-library discovery, a plain in-process pager, and truthful libc
  reporting. Dolly retains its real writable `/home/dolly/.local` user site.
- The browser acceptance test now installs `requests[socks]` and Pytest,
  imports `pdb`, Requests, and `_socket` together, proves raw socket creation
  fails, executes the separately compiled `pytest` PATH command, and runs a
  test. A subsequent probe enabled upstream `faulthandler`, so this now uses
  Pytest's normal built-in plugin set.
- Two clean rebuilds of this final target-identity recipe are byte-identical at
  337,877,711 bytes with SHA-256
  `cc91e7429c9faca9254815a69724e1a0b276bb4f5c02770f935393603df37571`.

### CPython faulthandler follow-up

- Enabled upstream `Modules/faulthandler.c` instead of introducing a
  Dolly-specific substitute. Its enable/disable state and synchronous Python
  traceback dump work in the browser; the one target exclusion is described
  below.
- The ABI validator rejected one `setrlimit` import from a private crash-test
  helper. Dolly has no core dumps or resource limits, so the reviewed target
  patch excludes only that meaningless suppression block rather than widening
  the substrate.
- Timeout dumps require a watchdog thread and therefore retain CPython's
  explicit `RuntimeError: unable to start watchdog thread` behavior on Dolly's
  single-thread target.
- Two clean rebuilds of the resulting Python image are byte-identical at
  337,912,769 bytes with SHA-256
  `6ad7a5eb705342ff73ec018ee0b8ed15c971c84564f051a581330fce1fe2eee8`.

### Bonnie requirements-file follow-up

- Bonnie 0.4 accepts `-r FILE` and `--requirement FILE`, preserving sequential
  requirement order while ignoring blank lines and conventional comments.
- Pip-specific directives such as `--index-url` are rejected with an exact
  file and line diagnostic. The browser test verifies that rejection occurs
  before the requested target directory is created.
- The existing Requests extras acceptance now runs from a real requirements
  file, while origin, pure-wheel, hash, transaction, and dependency policies
  remain unchanged.
- A broader live probe installs Black and its dependency graph, validates its
  separately compiled `/usr/bin/black` Wasm launcher, and uses it to reformat a
  Python file successfully.
- The Bonnie 0.4 image is reproducible: two clean builds produced identical
  337,917,093-byte snapshots with SHA-256
  `dcad2654d7e16c184c6b7796e332795b9b2bc87ad9e701cdb77860143f10018f`.

### CPython terminal follow-up

- Added a libc-independent terminal-mode substrate with only two mutable
  semantic bits: canonical input and echo. Ctrl+C remains unconditional
  command supervision in raw mode, so a program cannot disable recovery. The
  mode is in-Wasm state and introduces no browser import.
- Built upstream CPython `termios` and `tty` through a target adapter that maps
  `struct termios` and `TIOCGWINSZ` onto that substrate and Dolly's live
  terminal dimensions. Unsupported flow-control and break operations fail
  explicitly.
- Command boundaries restore the prior mode on normal return, command-local
  exit, and Ctrl-C. The live browser test deliberately leaves Python in raw
  mode, then proves the following invocation starts canonical again.
- Fixed CPython's embedded installation home to `/usr`, so invoking `python`
  through `PATH` finds the same standard library as an absolute-path recipe
  check instead of deriving the incorrect `/lib` prefix from `argv[0]`.

### Bonnie environment inspection

- Bonnie 0.5 adds offline `list`, `freeze`, and `show PACKAGE...` operations
  over CPython's installed distribution metadata. Agents can now inspect and
  reproduce the Python environment without another network request or browser
  capability.
- The browser test excludes CPython's bundled-but-not-installed ensurepip wheel
  from `freeze`, preventing a parser implementation detail from masquerading as
  an installed package.

### Snapshot build iteration

- `build:runtime` no longer deletes every image snapshot preemptively. After a
  successful build it retains only snapshots whose runtime build ID, inherited
  recipe chain, byte length, and SHA-256 all remain exact, pruning stale image
  pairs individually. Python-only work no longer forces unrelated default and
  gamedev browser rebuilds.

### Static platform census

- Factored the sealed system-snapshot decoder into one strict reusable module
  shared by packaging and audit tooling.
- Added `npm run census -- IMAGE`. It verifies the packaged snapshot digest,
  recognizes executables by `dolly.abi` plus `dolly_main`, excludes only shared
  machine/relocation imports, and produces exact typed operation-to-consumer
  and executable-to-operation reports.
- The current default, gamedev, and Python images contain respectively 57/317,
  58/335, and 60/374 ABI-stamped executables and distinct operations. This is
  explicitly link-time evidence, not a claim that every import executed.

### Agent utility follow-up

- Added unchanged pinned sbase `sort` and `uniq` as separately compiled `/bin`
  executables. They cover common data-reduction workflows without adding a
  shell builtin or browser surface.
- Probed upstream sbase `find`, but its dormant ownership predicates import
  `getgrgid` and `getgrnam`. Dolly deliberately has no user/group or permission
  model, so the ABI validator rejected it and the port was not retained. A
  future finite traversal tool should not smuggle that model into the libc
  surface.
- Rejected upstream sbase `xargs` unchanged: its implementation assumes
  fork/exec and parallel child collection. The later serial argument-batching
  work adds a deliberate Dolly command instead of an executable that
  immediately reaches `ENOSYS`.
- `uniq` demonstrated one missing pure-libc import, C99 `isblank(int)`. Added
  that exact typed operation to ABI v0 after review; it carries no browser or
  host authority and is independently contract-tested.

### Serial argument batching

- Added a separately compiled `/bin/xargs` for common agent pipelines. It
  resolves the requested executable through Dolly's PATH, invokes it through
  the in-Wasm spawn/wait lifecycle, and supports bounded whitespace or NUL
  input, `-n`, `-I`, `-r`, and `-t`.
- `-P 1` is accepted and larger values fail explicitly: version 0 gains useful
  compatibility without pretending to provide parallel processes. Ctrl-C and
  status 130 propagate instead of being flattened into an ordinary child
  failure.
- The final Python image is reproducible across two independent in-browser
  builds: 338,001,000 bytes with SHA-256
  `42cd63c763f826d19d702da449c217345fe1b3b4d9b22a6a606732d27262f8c6`.

### Bonnie consistency inspection

- Bonnie 0.6 adds an offline `check` operation. It evaluates installed
  requirements and environment markers, reports missing or incompatible
  distributions, and returns a failing status without using the network.
- Browser acceptance injects a deliberately broken distribution, observes the
  failure, removes it, and proves the real Requests/Pytest/Black environment is
  consistent again.

### Capability fingerprint

- Added `npm run fingerprint -- IMAGE`. It parses the real main Wasm, requires
  every typed browser import to belong to exactly one reviewed policy group,
  and fails unless the network group remains exactly
  `env.dolly_http_dispatch`.
- The authority digest covers those typed imports, the embedded command ABI
  stamp, and every normalized compiled WAT contract interface. The full
  fingerprint also covers runtime bytes, snapshot bytes, recipe chain, entry,
  retained-path manifest, and ABI source provenance. Deployment policy and
  secrets remain outside both identities.
- All three current images share authority SHA-256
  `6ad7a0ffbffde063075a69b8775d4938732cab7cb78ed0aeda33a13d97ffd22d`,
  while their full capsule fingerprints differ as expected.

### Descriptor epoch cleanup

- Added bounded command-boundary reclamation for descriptors opened by nested
  modules. Dolly snapshots descriptors below its explicit 4096-descriptor
  ceiling, restores routed standard streams, and closes only descriptors that
  did not belong to the parent epoch.
- This addresses accidental `open` leaks in the shared-everything model without
  pretending to isolate commands. Leaked libc `FILE` objects and adversarial
  high-numbered `dup2` remain explicitly outside this slice.
- The first Python rebuild caught an ownership distinction: persistent CPython
  legitimately retains descriptor-backed interpreter state. Persistent modules
  now promote newly retained descriptors into runtime-owned state so an outer
  ephemeral Bonnie invocation cannot reclaim them; ordinary command leaks are
  still closed.

### Final verified baseline

- Rebuilt the default, gamedev, and Python images against runtime build
  `sha256:ae6f8b4a6afaca6e3e7b31caf4396a50ed5135b85192a7ccfac7016f3ae1bf44`.
  Their filesystem snapshots are respectively 265,938,893, 288,206,660, and
  338,011,894 bytes.
- All 48 static tests and all three real-browser acceptance modes pass. The
  default mode includes an adversarial module that repeatedly leaks thousands
  of descriptors and mutates cwd/environment before a clean probe; gamedev
  delivered 36 guest frames over 45 animation frames in 750 ms; Python
  installed and exercised Requests, Pytest, and Black and detected deliberately
  broken installed metadata.
- The sealed-image census remains 57 executables/317 operations for default,
  58/335 for gamedev, and 60/374 for Python. All three have the same browser
  authority digest,
  `6ad7a0ffbffde063075a69b8775d4938732cab7cb78ed0aeda33a13d97ffd22d`.
- Two final first-principles Python rebuilds are byte-identical at 338,011,894
  bytes with SHA-256
  `c476240f2cb7924b4db1b402e799e8325fa224b9719972570eb6056e35a94b08`.

### Hard worker supervision

- Closed the cancellation case that cooperative lifecycle polling cannot:
  foreign Wasm with neither compiler-inserted edge safepoints nor a runtime
  interrupt hook. Ctrl+C gets a two-second cooperative grace period; a second
  Ctrl+C, or expiry with the same PID still active, terminates the runtime
  worker and reloads the page.
- Worker replacement restores the last explicitly saved named session, or the
  sealed base image for an unnamed route. Unsaved mutations cannot be captured
  after foreign code has monopolized the worker and are explicitly outside the
  guarantee.
- A real-browser adversarial test compiles an infinite C loop with edge polling
  disabled, checkpoints a workspace marker, hard-stops the loop, observes a
  fresh worker, and verifies the named in-Wasm filesystem checkpoint survived.
  This adds no Wasm import or guest capability.

### TypeScript and the Pi source loop

- Added a separately compiled `/usr/bin/tsc` that runs the unchanged official,
  digest-pinned TypeScript 5.9.3 compiler under Janis. Browser rebuild checks
  compile single-file and multi-file ESM entirely inside WasmFS, then execute
  the multi-file result with source-built QuickJS.
- Added a small decompression-only `/bin/gzip` linked to Dolly's source-built
  zlib. The Dollyfile therefore consumes the official untouched TypeScript npm
  `.tgz` through ordinary in-sandbox commands; archive extraction did not
  become a browser capability.
- Pinned Pi's exact npm `gitHead` commit and retained its source separately from
  the current runtime packages. The Dollyfile first compiled all 205
  coding-agent TypeScript modules, then expanded to all 495 modules in the
  telemetry, AI, agent, protocol, client, TUI, and coding-agent runtime
  workspace; source and output remain under `/usr/src/pi-source` for inspection.
- This is a parse/emit milestone, not yet a replacement runtime. The narrow
  target config uses `noCheck` and `types: []`, and the unbundled ESM graph
  still has bare imports for internal Pi packages and external npm packages.
  `/usr/bin/pi` continues to launch the checked host-esbuild artifact until a
  target-side filesystem resolver/bundler closes that explicit gap.

### Filesystem-only ESM packages

- Added Janis bare-package resolution without adding a package download path or
  browser capability. The QuickJS loader delegates only bare specifiers to the
  already loaded Janis policy, which searches ancestor `node_modules` paths and
  `/usr/lib/node_modules` entirely through WasmFS.
- The deliberately finite resolver supports package root/subpath imports plus
  exact, conditional, array-fallback, and single-wildcard `exports`. It rejects
  invalid specifiers, package-root escapes, absent exports, malformed
  manifests, missing files, and Node ESM builtins without an explicit adapter.
- A sequential Dollyfile fixture constructs a scoped package, resolves both its
  root and wildcard subpath, and executes it through source-built Janis in the
  real browser. The normal default Pi/browser suite remains green.
- This removes bundling as a fundamental requirement for target-emitted Pi.
  The concrete next work is to compile the remaining Pi workspace packages,
  install the pinned external runtime graph, and add only the ESM builtin
  adapters that execution reaches.
- Compiled that complete internal workspace next: 495 JavaScript modules across
  seven upstream packages. Dolly publishes each output with its unchanged
  manifest under `/usr/lib/node_modules/@earendil-works`; a prebuilt-image
  browser test imports the target-built telemetry package by its real scoped
  name. The remaining graph is now specifically external npm packages and ESM
  builtin adapters, not Pi's own source packages.

### Source-built Pi becomes the default

- Closed the measured Janis gaps reached by the complete unbundled CLI:
  JSON ESM modules, import/require condition ordering, relative CommonJS
  classification, `import.meta.resolve`, Node-shaped `process.argv`, and a
  deterministic WasmFS-only `fs.globSync`. Dollyfile fixtures cover each rule.
- Restored Pi's generated model catalogs from the exact pinned published
  package and copied its ordinary offline HTML/theme assets after target emit.
  These are data/resources, not host-executed application code.
- Built a separate preview command and proved its version path, complete
  interactive TUI, Dolly extension header, Slop `!` command, clean `/quit`,
  incremental streaming, and write/edit tool round trip in Chrome.
- Ran the target-emitted CLI against the real OpenRouter provider from an
  ephemeral browser profile. It completed the requested tool-use turn,
  installed an extension through Dolly HTTP, and exited cleanly.
- Promoted that exact module graph to `/usr/bin/pi`. The host-esbuild bundle,
  esbuild dependency, and preview command were removed. A source-visible list
  now selects 31 external packages, and packaging rejects any entry whose name,
  version, or integrity metadata disagrees with `package-lock.json`.
- Fixed a route-aware browser-test bug discovered by the direct SDK probe: the
  model fixture belongs at the origin-root `/fixture/` endpoint, not under an
  image route such as `/default/fixture/`. Both source-built and former bundled
  paths then passed the same deterministic agent test.

### Source-built Pi lifecycle closure

- The final browser matrix exposed a packaging defect that `pi --version`
  could not see: `PI_PACKAGE_DIR` still named the retired bundled layout, while
  themes and export assets lived beside the target-emitted package. Upstream
  `main()` therefore rejected during interactive initialization. Dolly now
  uses `/usr/lib/node_modules/@earendil-works/pi-coding-agent` as the ordinary
  package root and keeps its emitted `dist`, resources, docs, and examples
  together there.
- Added `PI_STARTUP_BENCHMARK=1 pi --offline --no-session` as a sequential
  Dollyfile `CHECK`. Every image build must initialize the real upstream TUI,
  theme, runtime services, and stdin before it can be sealed; a metadata-only
  version check can no longer certify a broken application layout.
- Made terminal input explicitly command-epoch-shaped. When resident Pi exits,
  Dolly drains queued records belonging to that PID, preserves resize state,
  consumes stale paste, and resets raw/canonical buffers before publishing the
  recovery Slop process. This closes the Ctrl+D keydown/keyup handoff without
  adding a browser import or signal surface.
- Tightened browser network diagnostics so policy denial, quota/fetch failure,
  and deliberate cancellation remain typed request results delivered to Wasm;
  only failures in the outer broker request chain become page diagnostics.

### Final source-built baseline

- Runtime build ID:
  `sha256:0321f56309dccc46f9c65dac6692766e55c1cf4d2bdc7813a593fb8dad0d44a7`.
- Final snapshots are default 325,762,061 bytes
  (`79b1618e2980103f8f43d89d12ccff95dcc17238dcc0df82697503b25c975cd7`),
  gamedev 348,014,348 bytes
  (`1ef5e706c0228b79247d4c91008c58f15f970b58d42d33e1ae3442d11f412f79`),
  and Python 397,819,582 bytes
  (`1879776702b6f477bf8696241df6e6ab9efe857a5c3cf4777046c0b024e9a02c`).
  The formal reproducibility command performed two additional independent
  default browser rebuilds; both were byte-identical to the packaged snapshot.
- The sealed census is 59 executables/317 operations for default, 60/335 for
  gamedev, and 62/374 for Python. All retain authority digest
  `6ad7a0ffbffde063075a69b8775d4938732cab7cb78ed0aeda33a13d97ffd22d`;
  their capsule fingerprints are respectively
  `40c8d7c5b6b9744c6704e1a2be5b8c660286d2b1a1ceeb8a3d78d8700a2960c4`,
  `09c36bede103c8613e4f1641dc4b61a3f635acaa19991d9f22fe66764cb67113`,
  and `f5c0ae7e2d553b7a6bca258ce6d71e02dcb8f46cd5ac26b66aa002b58c2ab2c0`.
- All 52 static tests pass. Real Chrome passes the full default proof,
  target-emitted Pi module-resolution proof, fixture streaming/thinking/tool
  turn, real OpenRouter extension install/restart/tool invocation, gamedev
  framebuffer and pacing proof, Python/Bonnie package proof, and hard worker
  replacement with session restore.

### TypeScript extension source loop

- Extended the deterministic Pi browser proof to author a typed `.ts`
  extension in `/workspace` after the first Pi invocation, compile it with the
  in-sandbox `/usr/bin/tsc`, and emit ordinary ESM directly into Pi's WasmFS
  extension directory.
- A fresh upstream Pi invocation in the same userspace loads that emitted
  module. The fixture model calls its `dolly_typescript_probe` tool and receives
  `DOLLY-TYPESCRIPT-EXTENSION-OK`, after which Ctrl+D returns cleanly to Slop.
  This proves source, compiler, package loader, extension API, model tool call,
  restart, and lifecycle together without host compilation or another network
  edge.

### External Pi package audit

- Expanded `npm run pi:census` from a size table into an auditable external
  runtime profile: all 31 entries now report exact lock integrity, declared
  license, retained root license files, lifecycle-script declarations, native
  addons, and nested Wasm payloads.
- The source profile has 6,058 files / 39,116,238 bytes, no `.node` addons, one
  no-op `preinstall`, no nested Wasm modules, and a retained root license file
  for every package.
- QuickJS/Janis has no source-map consumer, so one explicit suffix rule removes
  1,670 generated `.map` files / 13,569,865 bytes from the runtime archive. It
  now retains 4,388 files / 25,546,373 source bytes and packages to 29,004,288
  bytes with SHA-256
  `e395b7d88cbcc3afa020437507b309cc37a6f0b0645e038842a304a6aa6d2998`.
- The remaining 2,089 declaration, TypeScript-source, test, doc, or example
  candidates total 12,172,642 bytes. They stay retained: those are review
  candidates, not an unsupported claim of runtime unreachability, and license
  retention must be decided first.

### Nested-Wasm image boundary

- Probed Pi's sole nested Wasm dependency through the real Janis package
  resolver. Its CommonJS loader reads `photon_rs_bg.wasm` from WasmFS and then
  reaches exactly `ReferenceError: WebAssembly is not defined` at
  `WebAssembly.Module`; Pi catches this and would silently omit auto-resized
  images.
- Dolly now sets Pi's normal `images.autoResize` setting to false, so PNG,
  JPEG, GIF, and WebP data pass through unchanged, and removes Photon from the
  explicit package profile. This saves 2.27 MB of source payload without
  claiming nested Wasm support or adding a browser capability.
- The sequential Dollyfile executes Pi's target-emitted `processImage` module
  and requires a fixed PNG payload and MIME type to survive unchanged. The
  formal reproducibility run rebuilt that final image twice at exactly
  325,762,061 bytes and the same SHA-256 shown above.
- A future image-resize path must either compile the operation for Dolly or
  execute nested guest Wasm wholly inside the existing userspace boundary with
  bounded memory, cancellation, WasmFS-only bytes, and zero browser imports.

### Git helper boundary correction

- Verified in the sealed browser image that `git --exec-path` is
  `/usr/libexec/dolly` and the separately compiled `git-remote-http` module is
  present there. The normal clone diagnostic did not mean the helper artifact
  was absent.
- Found Git's first false gate in `run-command.c`: its PATH lookup requires a
  Unix execute bit even though Dolly deliberately has no permission model.
  The narrow target patch now treats any regular file as a command lookup
  candidate; the ABI-stamped module loader remains the actual validity check.
- This deliberately does not emulate `chmod` or claim clone support. It lets
  upstream Git reach the honest remaining boundary: `start_command()` needs a
  concurrently connected parent/helper protocol, while Dolly version 0 spawn
  is synchronous.

### Overnight shell utility expansion

- Confirmed that `/bin/true` and `/bin/false` were already separate unchanged
  pinned sbase executables, then added the actual missing traversal primitive:
  a separately compiled Dolly-owned `/bin/find`.
- `find` performs deterministic sorted, non-symlink-following traversal with
  boolean expressions, `-name`, `-path`, `-type`, `-empty`, depth bounds,
  `-depth`, `-prune`, `-print`, `-print0`, `-quit`, and serial
  `-exec ... {} ';'`/`+`. Ownership and permission predicates fail explicitly;
  no `chmod`, user database, group database, process, or browser capability was
  introduced. A target compile initially exposed libc `fnmatch` as an undeclared
  import, so bytewise wildcard matching remains command-local and the ABI did
  not grow.
- Added finite Dolly-owned `/bin/tail` and `/bin/tee`. Tail supports line/byte
  counts, `+N`, multiple inputs, and quiet/verbose headers but rejects follow
  mode because Dolly v0 has serial commands and no concurrent writer. Tee
  preserves arbitrary bytes and append mode but rejects `-i` because foreground
  Ctrl+C is unconditional lifecycle supervision.
- Added unchanged pinned sbase `/bin/basename`, `/bin/dirname`, and UTF-aware
  `/bin/tr`. All six new commands are ordinary files on `PATH`, compile from
  source inside Dolly, and passed exact ABI validation plus sequential startup
  checks.
- Fixed an older false-positive startup test: the early `xargs` checks invoked
  `printf` before sbase built it, and serial pipeline status masked the missing
  producer. Those checks now execute after `printf` exists.
- Added Dolly-owned `/bin/env` and `/bin/printenv`. `env` constructs an explicit
  copied child environment, searches its PATH, and executes through
  `dolly_spawn_env`; `-i`, `-u`, assignments, NUL output, status, and parent
  non-leakage are browser-tested. The custom `printenv` deliberately indexes
  `environ` because upstream sbase advances the process-global pointer, which
  is unsafe in Dolly's shared-everything address space.
- Added unchanged pinned sbase `/bin/cmp`, `/bin/date`, `/bin/mktemp`,
  `/bin/sha256sum`, and `/bin/sleep`. Target probes validated every module
  against the existing contract before the source subset was expanded; no
  browser import or runtime rebuild-ID change was required.
- The full Chrome proof now covers traversal/pruning, NUL-delimited
  `find | xargs`, both `-exec` modes, explicit permission rejection, tail
  selection/error behavior, binary-preserving tee/append behavior, `tr`, and
  path splitting. All 52 static tests and the complete default browser proof
  pass.
- Final snapshots for this pass are default 325,864,432 bytes
  (`61d9718f294548c635c02117462cda6fc0807094d1da88f44d9440b9a994db0f`),
  gamedev 348,113,249 bytes
  (`230b608232729a5b22133bd20d020f21ee9c32cdb43e5b110739d2435efe4bca`),
  and Python 397,918,483 bytes
  (`1fc0ff87d23267d92a434af6cf8ee8608b85c81499ba06c027f2f04322651e47`).
  Their census is 72/318, 73/336, and 75/374
  executables/distinct operations. Browser authority remains exactly
  `6ad7a0ffbffde063075a69b8775d4938732cab7cb78ed0aeda33a13d97ffd22d`.

### Final shell and command hardening

- Extended Slop with finite POSIX-shaped functions, groups and subshells,
  conditionals, loops, `case`, arithmetic and parameter expansion, field and
  pathname expansion, here-documents, `read`, `getopts`, local variables,
  descriptor redirection, `set -e`/`-x`/`pipefail`, and parse-only mode. The
  remaining gaps are stated explicitly in `docs/slop.md`; Slop is still not
  advertised as Bash or a complete POSIX `sh`.
- Added separately compiled bounded agent utilities including `command`,
  `xargs`, `find`, `du`, `dd`, `tail`, `tee`, `env`, `printenv`, `realpath`,
  `diff`, `patch`, `time`, `timeout`, `uname`, `hostname`, and `which`, plus a
  larger pinned sbase selection. The sealed default image now has 83 clean
  `/bin` files and 103 ABI-stamped executables. `true` and `false` remain
  independent upstream sbase binaries, not shell shortcuts.
- Tightened filesystem edge cases: `cp` no longer truncates its source through
  a destination symlink, trailing-slash copies and moves are bounded, recursive
  `rm` does not follow a trailing-slash symlink, and empty directory listings
  never pass a null base to `qsort`.
- The final default, gamedev, and Python snapshots contain 103/318, 104/336,
  and 106/374 executables/distinct operations. Their exact sizes and SHA-256
  digests are respectively 326,171,484 bytes
  (`e3b4c3d438e9f6342bd9068cf5753e6181ec700f28d504b5bfab2522649cbc7e`),
  348,400,363 bytes
  (`149e2daed8ca56e07322003060f4e1e93ce69334e14aca178f63df826b27e761`),
  and 398,205,597 bytes
  (`b0a6a1ae5c069343fe10421806ff049e5952eac7dee08aa2d71f56382d6fc54d`).
  Two independent default rebuilds were byte-identical. The authority digest
  is unchanged.
- All 52 static tests and the complete Chrome proof pass. Additional native
  evidence includes strict/analyzer compilation, sanitizer-backed malformed
  argument tests, 2,000 generated boolean-expression cases for `test`, native
  GNU `find` differential fixtures, generated Dash/Slop parser comparisons,
  stateful Slop semantics checks, and randomized ASan/UBSan parser fuzzing.
